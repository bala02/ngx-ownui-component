import { DropdownmenuDirective } from './dropdownmenu.directive';

describe('DropdownmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownmenuDirective();
    expect(directive).toBeTruthy();
  });
});
