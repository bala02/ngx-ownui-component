import { UiTooltipDirective } from './ui-tooltip.directive';

describe('UiTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new UiTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
